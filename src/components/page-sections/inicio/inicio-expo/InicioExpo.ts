import { defineComponent } from "vue";

export default defineComponent({
    name:'InicioExpo',
    setup(){

        const title = 'Inicio - Expo'

        return {
            title
        }
    }
})
