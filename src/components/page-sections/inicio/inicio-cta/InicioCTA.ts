import { defineComponent } from "vue";

export default defineComponent({
    name:'InicioCTA',
    setup(){

        const title = 'Inicio - CTA'

        return {
            title
        }
    }
})
