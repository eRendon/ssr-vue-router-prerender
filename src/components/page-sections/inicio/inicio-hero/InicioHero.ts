import { defineComponent } from "vue";

export default defineComponent({
    name:'InicioHero',
    setup(){

        const title = 'Inicio - Hero'

        return {
            title
        }
    }
})
